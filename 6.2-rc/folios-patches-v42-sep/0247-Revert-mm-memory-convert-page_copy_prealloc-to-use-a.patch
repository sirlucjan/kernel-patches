From 04c6ae0e40e4f7b3d1b55c3081feabbe311424d5 Mon Sep 17 00:00:00 2001
From: Piotr Gorski <lucjan.lucjanov@gmail.com>
Date: Mon, 16 Jan 2023 22:00:12 +0100
Subject: [PATCH 247/343] Revert "mm: memory: convert page_copy_prealloc() to
 use a folio"

This reverts commit 01f89537afd81490693d503c8f70fee59f7dee48.

Signed-off-by: Piotr Gorski <lucjan.lucjanov@gmail.com>
---
 mm/memory.c | 14 +++++++-------
 1 file changed, 7 insertions(+), 7 deletions(-)

diff --git a/mm/memory.c b/mm/memory.c
index 739c18414..3234bd3c2 100644
--- a/mm/memory.c
+++ b/mm/memory.c
@@ -962,19 +962,19 @@ static inline struct page *
 page_copy_prealloc(struct mm_struct *src_mm, struct vm_area_struct *vma,
 		   unsigned long addr)
 {
-	struct folio *new_folio;
+	struct page *new_page;
 
-	new_folio = vma_alloc_folio(GFP_HIGHUSER_MOVABLE, 0, vma, addr, false);
-	if (!new_folio)
+	new_page = alloc_page_vma(GFP_HIGHUSER_MOVABLE, vma, addr);
+	if (!new_page)
 		return NULL;
 
-	if (mem_cgroup_charge(new_folio, src_mm, GFP_KERNEL)) {
-		folio_put(new_folio);
+	if (mem_cgroup_charge(page_folio(new_page), src_mm, GFP_KERNEL)) {
+		put_page(new_page);
 		return NULL;
 	}
-	folio_throttle_swaprate(new_folio, GFP_KERNEL);
+	cgroup_throttle_swaprate(new_page, GFP_KERNEL);
 
-	return &new_folio->page;
+	return new_page;
 }
 
 static int
-- 
2.39.0.rc2.1.gbd5df96b79

