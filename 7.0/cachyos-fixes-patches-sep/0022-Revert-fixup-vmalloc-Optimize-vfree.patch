From 7c157ed8784b856c676154d1fa6cf2b85548c5b0 Mon Sep 17 00:00:00 2001
From: Eric Naim <dnaim@cachyos.org>
Date: Sun, 5 Apr 2026 02:27:02 +0800
Subject: [PATCH 22/30] Revert "fixup! vmalloc: Optimize vfree"

This reverts commit 72e0def98b8d2edd96804f93a6c58dd210ba9f4f.

Signed-off-by: Eric Naim <dnaim@cachyos.org>
---
 mm/vmalloc.c | 5 +++++
 1 file changed, 5 insertions(+)

diff --git a/mm/vmalloc.c b/mm/vmalloc.c
index c2873b4e4..5f393f26e 100644
--- a/mm/vmalloc.c
+++ b/mm/vmalloc.c
@@ -3442,6 +3442,7 @@ void vfree_atomic(const void *addr)
 void vfree(const void *addr)
 {
 	struct vm_struct *vm;
+	int i;
 
 	if (unlikely(in_interrupt())) {
 		vfree_atomic(addr);
@@ -3467,6 +3468,10 @@ void vfree(const void *addr)
 	/* All pages of vm should be charged to same memcg, so use first one. */
 	if (vm->nr_pages && !(vm->flags & VM_MAP_PUT_PAGES))
 		mod_memcg_page_state(vm->pages[0], MEMCG_VMALLOC, -vm->nr_pages);
+	if (!(vm->flags & VM_MAP_PUT_PAGES)) {
+		for (i = 0; i < vm->nr_pages; i++)
+			mod_lruvec_page_state(vm->pages[i], NR_VMALLOC, -1);
+	}
 	free_pages_bulk(vm->pages, vm->nr_pages);
 
 	if (!(vm->flags & VM_MAP_PUT_PAGES))
-- 
2.53.0

